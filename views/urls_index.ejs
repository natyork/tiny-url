<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Wee App</title>
  <link href="https://fonts.googleapis.com/css?family=Pacifico|Raleway" rel="stylesheet">
  <link rel="stylesheet" href="../css/styles.css">
</head>
<body>
  <header>
    <% include ./partials/_header %>
  </header>

  <h2>Wee URL</h2>


    <ul>
      <% for (key in urls) { %>
          <li>
            <form action="/urls/<%= key%>/delete" method="POST" ><%= key%>: <a href="/u/<%= key%>"><%= urls[key] %></a> <a href="/urls/<%= key%>">Edit</a>
              <input type="submit" value="Delete Wee Me!"/>
            </form>
          </li>
      <% }; %>
    </ul>

  <a href="/urls/new">Make a new Wee URL!</a>
</body>
</html>